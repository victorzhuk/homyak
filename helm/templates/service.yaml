apiVersion: v1
kind: Service
metadata:
  name: {{ include "homyak.fullname" . }}
  labels:
    {{- include "homyak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homyak.selectorLabels" . | nindent 4 }}
